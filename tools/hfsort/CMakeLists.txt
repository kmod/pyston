find_package(ZLIB REQUIRED)
if (ZLIB_FOUND)
    include_directories(${ZLIB_INCLUDE_DIRS})
    add_executable(hfsort "hfsort.cpp" "hfutil.cpp")
    target_link_libraries(hfsort ${ZLIB_LIBRARIES})
    add_executable(jit_sort "jitsort.cpp" "hfutil.cpp")
endif(ZLIB_FOUND)
